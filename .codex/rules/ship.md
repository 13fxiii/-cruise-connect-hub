# Cruise OS v4 ship rules

- Never ship without passing preinstall, prebuild, risk, build, and monitor checks.
- Block when Supabase fallback patterns are detected in `src/lib`.
- Keep changes deploy-safe and backwards compatible.
